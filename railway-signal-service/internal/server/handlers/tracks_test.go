package handlers

import "testing"

func TestTracks(t *testing.T) {
	// TODO these will just look very similar to the signals tests.
	// Perhaps the handlers could be combined using Generics in some places.
	t.Skip("Not implemented")
}
